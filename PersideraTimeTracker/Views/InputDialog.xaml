<Window x:Class="PersideraTimeTracker.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Project" Height="170" Width="360"
        WindowStyle="None" AllowsTransparency="False"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#191919" FontFamily="Segoe UI">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="56"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#1F1F1F" MouseLeftButtonDown="TitleBar_Drag">
            <TextBlock x:Name="HeaderText" Text="NEW PROJECT" Foreground="#9A9A9A" FontSize="11"
                       FontWeight="SemiBold" VerticalAlignment="Center" Margin="16,0"/>
            <Button Content="&#10005;" Style="{StaticResource CloseButton}" Width="44" Height="32"
                    HorizontalAlignment="Right" Click="Cancel_Click"/>
        </Grid>

        <StackPanel Grid.Row="1" Margin="20,16">
            <TextBlock x:Name="PromptText" Text="Project name" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="Input" Margin="0,4,0,0" Height="34"/>
        </StackPanel>

        <Grid Grid.Row="2" Background="#1F1F1F">
            <Border BorderBrush="#2A2A2A" BorderThickness="0,1,0,0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center"
                        Margin="0,0,16,0">
                <Button Content="Cancel" Padding="16,6" Margin="0,0,8,0" Click="Cancel_Click"/>
                <Button Content="Create" Padding="20,6" Style="{StaticResource EmberButton}" Click="Ok_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
